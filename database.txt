-- Create database dbv1;

use dbv1;


drop table data;

CREATE TABLE data (
	id int AUTO_INCREMENT primary key not null,
    name char(15),
    content text,
    is_new tinyint(1) default 1,
    time timestamp default current_timestamp,
    office char(5),
    des_grade char(3),
    des_class char(1)
);


create Table tea_infor (
	id int AUTO_INCREMENT primary key,
    lineID varchar(40) NOT NULL,
    teacher varchar(20) not null,
    fromWhere varchar(20) not null,
	isAdmin boolean DEFAULT False,
    verifyStat tinyint default 0
);

-- insert into data (name, content, office, des_grade, des_class) 
-- values ("林佳生", "目前沒有，繼續打廢話", "教務處", "11", "3");
insert into data (name, content, office, des_grade, des_class) 
values ("你好", "aasd", "那是誰", "08", "5");drop table data;

select * from tea_infor;
select * from data;


alter table data add column group_send char(1);